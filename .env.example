# Frontend port (external port mapping)
# The container internally uses port 3000, but you can map it to any external port
FRONTEND_PORT=3001

# Backend port (if needed in the future)
# BACKEND_PORT=8000
